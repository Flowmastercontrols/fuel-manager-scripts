# =============================================================================
# FuelManager Kiosk — Tested System Reference
# -----------------------------------------------------------------------------
# Single source of truth for the Raspberry Pi hardware and OS version the
# kiosk has been verified against. Sourced by install-raspberry.sh and
# check-raspberry.sh. Mirrored in TypeScript at
# src/main/constants/testedSystem.ts — keep both in sync.
#
# Update only after a full regression pass on a freshly flashed image.
# =============================================================================

EXPECTED_MODEL="Raspberry Pi 5"
EXPECTED_OS_ID="debian"
EXPECTED_OS_VERSION_ID="12"
EXPECTED_OS_CODENAME="bookworm"
EXPECTED_RPI_IMAGE_DATE="2025-05-13"
EXPECTED_KERNEL_MAJOR_MINOR="6.12"
EXPECTED_ARCH="aarch64"
